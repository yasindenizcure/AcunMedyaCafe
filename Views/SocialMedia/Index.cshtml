@model List<AcunMedyaCafe.Entities.SocialMedia>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="content-wrapper">
    <div class="page-header">
        <h3 class="page-title"> Sosyal Medya Tablosu </h3>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="#">Tablo</a></li>
                <li class="breadcrumb-item active" aria-current="page">Sosyal Medya</li>
            </ol>
        </nav>
    </div>
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Platform Adı</th>
                                <th>Hesap Link</th>
                                <th>Güncelle</th>
                                <th>Sil</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Title</td>
                                    <td>@item.Url</td>
                                    <td><a href="/SocialMedia/UpdateSocialMedia/@item.SocialMediaId" class="btn btn-gradient-warning">Güncelle</a></td>
                                    <td><a href="/SocialMedia/DeleteSocialMedia/@item.SocialMediaId" class="btn btn-gradient-danger">Sil</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <br />
                    <a href="/SocialMedia/AddSocialMedia" class="btn btn-primary"><i class="fa fa-plus-circle"></i> Yeni Sosyal Medya</a>
                </div>
            </div>
        </div>

    </div>
</div>